# Expo project guidance

This repository currently uses Expo SDK 54.

Before changing Expo-specific code, read the exact versioned documentation at https://docs.expo.dev/versions/v54.0.0/ and keep package versions compatible with SDK 54 unless an upgrade is explicitly planned and tested.

Run `npm run typecheck` before publishing changes.
